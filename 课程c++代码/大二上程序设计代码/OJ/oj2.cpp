#include <iostream>
#include <cmath>
using namespace std;

int main()
{
	int V, N;
	cin >> V >> N;
	int *c = new int[N];
	int *w = new int[N];
	for (int i = 0; i < N; i++)
	{
		cin >> c[i] >> w[i];
	}

	return 0;
}